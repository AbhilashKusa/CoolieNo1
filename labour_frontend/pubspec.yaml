name: labour_frontend
description: "🚀 Flutter frontend for labor marketplace with secure authentication and a user-centric, psychologically optimized color scheme."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  provider: ^6.0.0
  fluttertoast: ^8.1.2
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # 📁 Add asset paths here if needed, e.g.:
    # - assets/icons/
